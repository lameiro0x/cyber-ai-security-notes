# Indice

- [Parte teorica](<#parte-teorica>)
	- [Monitorizacion pasiva](<#monitorizacion-pasiva>)
	- [Monitorizacion activa](<#monitorizacion-activa>)
- [Parte practica](<#parte-practica>)
	- [Preparacion](<#preparacion>)
	- [Monitorizacion pasiva](<#monitorizacion-pasiva-1>)
	- [Creanod nuestro propia Guardrail de monitorizacion activa](<#creanod-nuestro-propia-guardrail-de-monitorizacion-activa>)


Todos los comandos del **Jupyter Notebook** estan en [Comandos Passive and active monitoring](<./Comandos/Comandos Passive and active monitoring.md>)

# Parte teorica

## Monitorizacion pasiva

> Se hace despues de que la interaccion con el modelo LLM se haya completado

## Monitorizacion activa

> Puede ocurrir median la ejecucion del LLM

![pasted_image_20260505120949.png](<../images/pasted_image_20260505120949.png>)

---
# Parte practica

## Preparacion

Hacemos los imports

```python
import pandas as pd
```

```python
import whylogs as why
```

```python
import helpers
```

```python
from langkit import llm_metrics
```

E inicializamos las metricas:

```python
llm_metrics.init()
```

Hacemos mas imports:

```python
from whylogs.experimental.core.udf_schema import register_dataset_udf
```

```python
from whylogs.experimental.core.udf_schema import udf_schema
```

Y ahora cargamos en el esquemas todas las metricas:

```python
llm_schema = udf_schema()
```

Y tambien podemos crear un nuevo logger par arecoger los logs de forma mas sencilla:

```python
llm_schema = udf_schema()
```


```python
llm_logger = why.logger(schema=udf_schema())
```

```python
llm_logger = why.logger(
    model = "rolling",
    interval = 1,
    when = "H",
    schema = udf_schema()
)
```

**Note**: To accessthe WhyLabs platform that was built in the previous lessons: https://hub.whylabsapp.com/resources/model-1/profiles?profile=ref-EBT5yDFL0lyq0r93&sessionToken=session-pPdc5R9m

## Monitorizacion pasiva

![pasted_image_20260505120325.png](<../images/pasted_image_20260505120325.png>)

![pasted_image_20260505120342.png](<../images/pasted_image_20260505120342.png>)

Podemos ver demos globales cambiando a `Project dashboard` arriba a la izquierda y en `Select Organizations` cambiar a `Golbal Demo Organization`

![pasted_image_20260505120608.png](<../images/pasted_image_20260505120608.png>)

![pasted_image_20260505120623.png](<../images/pasted_image_20260505120623.png>)

Y aqui podemos ver demos globales

![pasted_image_20260505120654.png](<../images/pasted_image_20260505120654.png>)

Vamos a ir a la primera

![pasted_image_20260505120738.png](<../images/pasted_image_20260505120738.png>)

Y vamos a ir a Dashboards

![pasted_image_20260505120754.png](<../images/pasted_image_20260505120754.png>)

podemos ver muchas metricas como hemos visto en lecciones anteriores

![pasted_image_20260505120909.png](<../images/pasted_image_20260505120909.png>)


## Creanod nuestro propia Guardrail de monitorizacion activa

### Preparacion

Primero hacemos los imports y cargamos el modelo con una key:

```python
import openai
```

```python
openai.api_key = helpers.get_openai_key()
```

Ahora vamos a cargar el logger de forma activa:

```python
active_llm_logger = why.logger()
```

Vamos a crear una funcion donde se haga una peticion del usuario donde pide recetas, y haremos que nuestreo logger en esa funcion trabaje sobre esa peticion:

```python
def user_request():
    # Take request
    request = input("\nEnter your desired item to make a recipe" \
                    "(or 'quit'):")
    if request.lower() == "quit":
        raise KeyboardInterrupt()
        
    # Log request
    active_llm_logger.log({"request": request})

    return request
```

Y ahora vamos a transformar con una funcion esa request en un prompt hacia un modelo y con el logger monitorizar:

```python
def prompt_llm(request):
    # Transform prompt
    prompt = f"""Please give me a short recipe for creating"\
    the following item in up to 6 steps. Each step of the recipe "\
    should be summarized in no more than 200 characters."\
    Item: {request}"""

    # Log prompt
    active_llm_logger.log({"prompt": prompt})

    # Collect response from LLM
    response = openai.ChatCompletion.create(
        model = "gpt-3.5-turbo",
        messages = [{
            "role": "system",
            "content": prompt
        }]
    )["choices"][0]["message"]["content"]

    # Log response
    active_llm_logger.log({"response": response})

    return response
```

Ahora vamos a crear una funcion para cuando la respuesta es exitosa:

```python
def user_reply_success(request,response):
    # Create and print user reply
    reply = f"\nSuccess! Here is the recipe for"\
            f"{request}:\n{response}"
    print(reply)

    #Log reply
    active_llm_logger.log({"reply": reply})
```

Y otra para cuando falla:

```python
def user_reply_failure(request = "your request"):
    # Create and print user reply
    reply = ("\nUnfortunately, we are not able to provide a recipe for " \
            f"{request} at this time. Please try Recipe Creator 900 " \
            f"in the future.")
    print(reply)

    #Log reply
    active_llm_logger.log({"reply": reply})
```

Ahora para utilizar estas funciones y se activen correntamente vamos a manejarlas con excepciones.

Primero creamos la clase:

```python
class LLMApplicationValidationError(ValueError):
    pass
```

Y ahora creamos una funcion que corra mientras pinesa el modelo y que se activa cuando todo va bien, cuando se interrumpe por teclado o falla.

Una vez ejecutado esto le podemos pedir al modelo recetas de comidas y todo se ira monitorizando

![pasted_image_20260505121854.png](<../images/pasted_image_20260505121854.png>)

```python
while True:
    try:
        request = user_request()
        response = prompt_llm(request)
        user_reply_success(request, response)
    except KeyboardInterrupt:
        break
    except LLMApplicationValidationError:
        user_reply_failure(request)
        break
```

### Validadores

Pero que pasa si tenemos otro tipo de errores, vamos a verlo:

```python
from whylogs.core.relations import Predicate
from whylogs.core.metrics.condition_count_metric import Condition
from whylogs.core.validators import ConditionValidator
```

Para ello vamos a crear una funcion que cogera un validador y una condicion, y levantara un error si ese validador salta con esa condicion:

```python
def raise_error(validator_name, condition_name, value):
    raise LLMApplicationValidationError(
        f"Failed {validator_name} with value {value}."
    )
```

Ahora para probarlo vamos a crear la condicion:

Que va a ser que cuando el valor de validador es menor a 0.3

```python
low_condition = {"<0.3": Condition(Predicate().less_than(0.3))}
```

Ahora vamos a crear varios validadores, uno para toxicidad:

```python
toxicity_validator = ConditionValidator(
    name = "Toxic",
    conditions = low_condition,
    actions = [raise_error]
)
```

Otro para rechazos:

```python
refusal_validator = ConditionValidator(
    name = "Refusal",
    conditions = low_condition,
    actions = [raise_error]
)
```

Ahora vamos a definir las metricas en las que se van a basar estos validadores. estas metricas las vemos en lecciones anteriores:

```python
llm_validators = {
    "prompt.toxicity": [toxicity_validator],
    "response.refusal_similarity": [refusal_validator]
}
```

Ahora vamos a crear el logger activo:

```python
active_llm_logger = why.logger(
    model = "rolling",
    interval = 5,
    when = "M",
    base_name = "active_llm",
    schema = udf_schema(validators = llm_validators)
)
```

Podemos probarlo con respuestas concretas fuera de la aplicacion:

> **Note**: the next code cell is expected to return an 'LLMApplicationValidationError '. 

```python
active_llm_logger.log(
    {"response":"I'm sorry, but I can't answer that."}
)
```

![pasted_image_20260505122847.png](<../images/pasted_image_20260505122847.png>)

Y podemos ver los valores de los validadores y porque da el error

Ahora podemos hacerlo mientras ejecutamos una aplicacion, podemos usar el mismo codigo del bucle anterior con las recetas de comida:

```python
while True:
    try:
        request = user_request()
        response = prompt_llm(request)
        user_reply_success(request, response)
    except KeyboardInterrupt:
        break
    except LLMApplicationValidationError:
        user_reply_failure(request)
        break
```

![pasted_image_20260505122950.png](<../images/pasted_image_20260505122950.png>)

Podemos hacer recertas para comida o conceptos como el exito 

![pasted_image_20260505123015.png](<../images/pasted_image_20260505123015.png>)

Y podemos hacer saltar los validadores con rechazos pidiendo la receta de una bomba:

![pasted_image_20260505123046.png](<../images/pasted_image_20260505123046.png>)



