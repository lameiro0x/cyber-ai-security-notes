# Indice

- [Parte teorica](#parte-teorica)
	- [Diferentes tipos de metricas](#diferentes-tipos-de-metricas)
	- [Alucinaciones y relevancia](#alucinaciones-y-relevancia)
	- [Relevancia prompt-response](#relevancia-prompt-response)
		- [BLEU score](#bleu-score)
		- [BERT score](#bert-score)
	- [Reponse self-similarity](#reponse-self-similarity)
		- [Sentence embedding cosine distance](#sentence-embedding-cosine-distance)
		- [LLM self-evaluation](#llm-self-evaluation)
- [Parte practica](#parte-practica)
	- [Relevancia Prompt-response](#relevancia-prompt-response-1)
		- [BLEU score](#bleu-score-1)
		- [BERT score](#bert-score-1)
	- [Response self-similarity](#response-self-similarity)
		- [Sentence embedding cosine distance](#sentence-embedding-cosine-distance-1)
		- [LLM self-evaluation](#llm-self-evaluation-1) 


Todos los comandos del **Jupyter Notebook** estan en [Comandos Alucionaciones](/Quality%20and%20Safety%20for%20LLM%20Applications/Comandos/Comandos%20Alucionaciones.md)
# Parte teorica
## Diferentes tipos de metricas

![pasted_image_20260505103356.png](/images/pasted_image_20260505103356.png)

## Alucinaciones y relevancia

![pasted_image_20260505001507.png](/images/pasted_image_20260505001507.png)

## Relevancia prompt-response

> Prompt vs respuesta

Veremos algunas metricas que compara promtp y respuesta para medir la relevancia
### BLEU score

Relacionada similitudes en los tokens

![pasted_image_20260505001852.png](/images/pasted_image_20260505001852.png)

Puede buscar por unigramas ,es decir palabras sueltas 

![pasted_image_20260505002053.png](/images/pasted_image_20260505002053.png)

O por bigramas, pares de palabras:

![pasted_image_20260505002211.png](/images/pasted_image_20260505002211.png)

### BERT score

Busca similitudes semanticas a traves del contexto, usanod semantica con un algoritmo to raro de pares con matches

![pasted_image_20260505002714.png](/images/pasted_image_20260505002714.png)

## Reponse self-similarity
### Sentence embedding cosine distance

> Respuesta vs respuesta vs respuesta para el mismo prompt

![pasted_image_20260505102447.png](/images/pasted_image_20260505102447.png)

### LLM self-evaluation

Ahora lo que vamos a hacer es pasarle a un modelo LLM que compare lo similares que son las frases entre si para que evalue (no tiene que ser el mismo LLM)

![pasted_image_20260505103448.png](/images/pasted_image_20260505103448.png)


---
# Parte practica

Hacemos los imports pertinentes y cargar el archivo cvs con los chats con pandas:

```python
import helpers
```

```python
import evaluate
```

```python
import pandas as pd
```

```python
pd.set_option('display.max_colwidth', None)
```

```python
chats = pd.read_csv("./chats.csv")
```

## Relevancia Prompt-response 

### BLEU score

Lo inicializamos

```python
bleu = evaluate.load("bleu")
```

Vemos y cogemos un chat

```python
chats[5:6]
```

![pasted_image_20260505001956.png](/images/pasted_image_20260505001956.png)


```python
bleu.compute(predictions=[chats.loc[2, "response"]], 
             references=[chats.loc[2, "prompt"]], 
             max_order=2)
```

Ahora llamamos a la funcion blue: (**nos centraremos en la primera metrica**)

![pasted_image_20260505002033.png](/images/pasted_image_20260505002033.png)

Ahora vamos a crear una nueva metrica par apoder medir el bleu score

```python
from whylogs.experimental.core.udf_schema import register_dataset_udf
```

Creamos la funcion `bleu_score()` que usara la funcion de antes de bleu para hacer el calculo

```python
@register_dataset_udf(["prompt", "response"], 
                      "response.bleu_score_to_prompt")


def bleu_score(text):
  scores = []
  for x, y in zip(text["prompt"], text["response"]):
    scores.append(
      bleu.compute(
        predictions=[x], 
        references=[y], 
        max_order=2
      )["bleu"]
    )
  return scores
```

**Note**: To view the next visual, you may have to either hide the left-side menu bar or widen the notebook towards the right.

Ahora vamos a ver los resultados de la metrica(mas cerca de 0 mas alucinacion)

```python
helpers.visualize_langkit_metric(
    chats, 
    "response.bleu_score_to_prompt", 
    numeric=True)
```

![pasted_image_20260505002438.png](/images/pasted_image_20260505002438.png)

Ahora vamos a ejemplosd e los que mas tienden a ser alucionaciones

![pasted_image_20260505002523.png](/images/pasted_image_20260505002523.png)

### BERT score

Cargamos el modelo:

```python
bertscore = evaluate.load("bertscore")
```

Y usamos su funcion para medir 

```python
bertscore.compute(
    predictions=[chats.loc[2, "prompt"]],
    references=[chats.loc[2, "response"]],
    model_type="distilbert-base-uncased")
```

![pasted_image_20260505002803.png](/images/pasted_image_20260505002803.png)

Ahora como hicimos antes usaremos esta funcion para crear una nueva metrica para medir mejor

Como vemos lo hacemos a traces de la funcion `bert_score()`

```python
@register_dataset_udf(["prompt", "response"], "response.bert_score_to_prompt")
def bert_score(text):
  return bertscore.compute(
      predictions=text["prompt"].to_numpy(),
      references=text["response"].to_numpy(),
      model_type="distilbert-base-uncased"
    )["f1"]
```

**Note**: To view the next visual, you may have to either hide the left-side menu bar or widen the notebook towards the right.

Ahora podemos visualizarlo: (donde los valores con menos score son los que mas pueden ser alucinacion)

```python
helpers.visualize_langkit_metric(
    chats, 
    "response.bert_score_to_prompt", 
    numeric=True)
```

![pasted_image_20260505002906.png](/images/pasted_image_20260505002906.png)

Y podemos ver los ejemplos que tienden mas a alucionaciones:

```python
helpers.show_langkit_critical_queries(
    chats, 
    "response.bert_score_to_prompt", 
    ascending=True)
```

![pasted_image_20260505003004.png](/images/pasted_image_20260505003004.png)

Ahrora vamos a hacer un poco de evalucacion, para ello primero lo tenemos que pasar a udf asi:

> udf como que junta lo de los chats y los resultados de los analisis previos

```python
from whylogs.experimental.core.udf_schema import udf_schema
```

```python
annotated_chats, _ = udf_schema().apply_udfs(chats)
```

**Note**: To view the next visuals, you may have to either hide the left-side menu bar or widen the notebook towards the right.

Y ahora podemos filtrar los chats en funcion de su BERT score, por ejemplo con menos o igual a 0.75 y ver su stest 

```python
helpers.evaluate_examples(
  annotated_chats[annotated_chats["response.bert_score_to_prompt"] <= 0.75],
  scope="hallucination")
```

![pasted_image_20260505003357.png](/images/pasted_image_20260505003357.png)

## Response self-similarity

Para esto tenemos que usar un .csv que tiene varias respuestas ora un mismo prompt

```python
chats_extended = pd.read_csv("./chats_extended.csv")
```

```python
chats_extended.head(5)
```

### Sentence embedding cosine distance

Hacemos los imports e inicializamos con un modelo de nuestra eleccion

```python
from sentence_transformers import SentenceTransformer
```

```python
model = SentenceTransformer('all-MiniLM-L6-v2')
```

Para embedding una frase hacemos: 

> Aqui embedding es transformas la frase en un vector de números que captura su significado semántico.

```python
model.encode("This is a sentence to encode.")
```

Para comparar varias frases embedeadas 

Primero hacemos unos imports 

```python
from sentence_transformers.util import pairwise_cos_sim
```

Y preparamos lo siguiente, donde creamos la funcion `sentence_embedding_selfsimilarity()` para embedear todas las respuestas

Debajo hacemos la comparacion por pares de respuestas y calculamos el valor final

```python
@register_dataset_udf(["response", "response2", "response3"], 
                      "response.sentence_embedding_selfsimilarity")
def sentence_embedding_selfsimilarity(text):
  response_embeddings = model.encode(text["response"].to_numpy())
  response2_embeddings = model.encode(text["response2"].to_numpy())
  response3_embeddings = model.encode(text["response3"].to_numpy())
  
  cos_sim_with_response2 = pairwise_cos_sim(
    response_embeddings, response2_embeddings
    )
  cos_sim_with_response3  = pairwise_cos_sim(
    response_embeddings, response3_embeddings
    )
  
  return (cos_sim_with_response2 + cos_sim_with_response3) / 2
```

Y ahora lo ejecutamos y vemos los valores

```python
sentence_embedding_selfsimilarity(chats_extended)
```

![pasted_image_20260505103122.png](/images/pasted_image_20260505103122.png)

**Note**: To view the next visual, you may have to either hide the left-side menu bar or widen the notebook towards the right.

Para ver una grafica de estos valores: (mas cerca de 0 mas alucinacion)

```python
helpers.visualize_langkit_metric(
    chats_extended, 
    "response.sentence_embedding_selfsimilarity", 
    numeric=True)
```

![pasted_image_20260505103143.png](/images/pasted_image_20260505103143.png)

Para ver ejemplos de los mas cercano a alucinaciones:

```python
helpers.show_langkit_critical_queries(
    chats_extended, 
    "response.sentence_embedding_selfsimilarity", 
    ascending=True)
```

![pasted_image_20260505103304.png](/images/pasted_image_20260505103304.png)

Ahora vamos a transformar como antes a udf para analizar los chats


```python
annotated_chats, _ = udf_schema().apply_udfs(chats_extended)
```


```python
annotated_chats.head(5)
```

### LLM self-evaluation

Hacemos los imports e inicializamos:

```python
import openai
```

```python
import helpers
```

```python
openai.api_key = helpers.get_openai_key()
openai.base_url = helpers.get_openai_base_url()
```

Ahora vamos a crear una funcion donde le pongamos un modelo un prompt para que haga lo que queremos:

```python
def prompt_single_llm_selfsimilarity(dataset, index):
    return openai.ChatCompletion.create(
        model="gpt-3.5-turbo",
        messages=[{
            "role": "system",
            "content": f"""You will be provided with a text passage \
            and your task is to rate the consistency of that text to \
            that of the provided context. Your answer must be only \
            a number between 0.0 and 1.0 rounded to the nearest two \
            decimal places where 0.0 represents no consistency and \
            1.0 represents perfect consistency and similarity. \n\n \
            Text passage: {dataset['response'][index]}. \n\n \
            Context: {dataset['response2'][index]} \n\n \
            {dataset['response3'][index]}."""
        }]
    )
```

Y llamamos a la funcion con nuestros chat y el inidice coger el conjunto de respuestas y prompt que queremos:

```python
prompt_single_llm_selfsimilarity(chats_extended, 0)
```

![pasted_image_20260505103852.png](/images/pasted_image_20260505103852.png)

Nos da el objeto en json de OpenAi, nosotros lo que **queremos es el valor de content**, en este caso 0.75

Podemos filtrar por el valor:

```python
chats_extended[
chats_extended["response.prompted_selfsimilarity"] <= 0.8
]
```