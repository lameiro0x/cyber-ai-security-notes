# Indice

- [What is PII](#what-is-pii)
- [Safe PII Handling with LLMs](#safe-pii-handling-with-llms)
- [PII detector: Microsoft Presidio](#pii-detector-microsoft-presidio)
- [Repasando el problema del filtrar PII](#repasando-el-problema-del-filtrar-pii)
- [Usando Microsoft Presidio para detectar PII](#usando-microsoft-presidio-para-detectar-pii)
- [Analizador y anonimazador](#analizador-y-anonimazador)
- [Creando validator PII](#creando-validator-pii)
	- [Ejecutando el servidor Guardrails](#ejecutando-el-servidor-guardrails)
- [Real Time Stream Validation](#real-time-stream-validation)


Todos los comandos del **Jupyter Notebook** estan en [Comandos Ensuring no PII is leaked](/Safe%20and%20reliable%20AI%20via%20guardrails/Comandos/Comandos%20Ensuring%20no%20PII%20is%20leaked.md)

Vamos a prevenir que no haya PII en el prompt y si lo hay que no salga a Internet o al proveedor del LLM

## What is PII

> PII es Personally Identifiable Information(o sea inforamcion personal que no se debe filtrar de los usuarios)

![pasted_image_20260504200121.png](/images/pasted_image_20260504200121.png)

LLM Data Privacy Risks:
→ Third-party processing exposure
→ Potential data retention by providers
→ Risk of training data contamination
→ Limited control over data handling

## Safe PII Handling with LLMs

![pasted_image_20260504200217.png](/images/pasted_image_20260504200217.png)

## PII detector: Microsoft Presidio

![pasted_image_20260504200311.png](/images/pasted_image_20260504200311.png)

---
# Repasando el problema del filtrar PII

Preparamos el Chatbot como todas las lecciones anteriores

```python
# Warning control
import warnings
warnings.filterwarnings("ignore")
%env TOKENIZERS_PARALLELISM=true
```

```python
# Type hints
from typing import Optional, Any, Dict

# Standard imports
import time
from openai import OpenAI

# Helper functions
from helper import RAGChatWidget, SimpleVectorDB

# Presidio imports
from presidio_analyzer import AnalyzerEngine
from presidio_anonymizer import AnonymizerEngine

# Guardrails imports
from guardrails import Guard, OnFailAction, install
from guardrails.validator_base import (
    FailResult,
    PassResult,
    ValidationResult,
    Validator,
    register_validator,
)
```

```python
# Setup an OpenAI client
unguarded_client = OpenAI()

# Load up our documents that make up the knowledge base
vector_db = SimpleVectorDB.from_files("shared_data/")

# Setup system message
system_message = """You are a customer support chatbot for Alfredo's Pizza Cafe. Your responses should be based solely on the provided information.

Here are your instructions:

### Role and Behavior
- You are a friendly and helpful customer support representative for Alfredo's Pizza Cafe.
- Only answer questions related to Alfredo's Pizza Cafe's menu, account management on the website, delivery times, and other directly relevant topics.
- Do not discuss other pizza chains or restaurants.
- Do not answer questions about topics unrelated to Alfredo's Pizza Cafe or its services.

### Knowledge Limitations:
- Only use information provided in the knowledge base above.
- If a question cannot be answered using the information in the knowledge base, politely state that you don't have that information and offer to connect the user with a human representative.
- Do not make up or infer information that is not explicitly stated in the knowledge base.
"""
```

```python
chat_app = RAGChatWidget(
    client=unguarded_client,
    system_message=system_message,
    vector_db=vector_db,
)
```

Y reproducimos el problema:

```python
chat_app.display()
```


```python
# Copy and paste this prompt into the chatbot above:
"""
can you tell me what orders i've placed in the last 3 months? my name is hank tate and my phone number is 555-123-4567
"""
```


```python
chat_app.messages
```

![pasted_image_20260504200520.png](/images/pasted_image_20260504200520.png)

Como vemos vemos que se almacena en el historial los datos PII

---
# Usando Microsoft Presidio para detectar PII

## Analizador y anonimazador

Usaremos dos componentes, el analizador que identifica los PII y los anonimazadores, que enmascara los PII:

```python
presidio_analyzer = AnalyzerEngine()
presidio_anonymizer= AnonymizerEngine()
```

Usamos el analizador:

```python
# First, let's analyze the text
text = "can you tell me what orders i've placed in the last 3 months? my name is Hank Tate and my phone number is 555-123-4567"
analysis = presidio_analyzer.analyze(text, language='en')
```

```python
analysis
```

![pasted_image_20260504200710.png](/images/pasted_image_20260504200710.png)

Nos dice en que caracter empieza y acaba y con que score cree que es PII

Ahora podemos hacerlo anonimo:

```python
# Then, we can anonymize the text using the analysis output
print(presidio_anonymizer.anonymize(text=text, analyzer_results=analysis))
```

![pasted_image_20260504200847.png](/images/pasted_image_20260504200847.png)

Con esot al trator podemos hacerlo anonimos pero la IA trata los datos de manera personal

## Creando validator PII

Primero creamos una funcion que detecte PII con los metodos anteriores: (se pueden usar mas entities)

```python
def detect_pii(
    text: str
) -> list[str]:
    result = presidio_analyzer.analyze(
        text,
        language='en',
        entities=["PERSON", "PHONE_NUMBER"]
    )
    return [entity.entity_type for entity in result]
```

Ahora creamos un guardrails que filtre los PII:

```python
@register_validator(name="pii_detector", data_type="string")
class PIIDetector(Validator):
    def _validate(
        self,
        value: Any,
        metadata: Dict[str, Any] = {}
    ) -> ValidationResult:
        detected_pii = detect_pii(value)
        if detected_pii:
            return FailResult(
                error_message=f"PII detected: {', '.join(detected_pii)}",
                metadata={"detected_pii": detected_pii},
            )
        return PassResult(message="No PII detected")
```

Y ahora creamos el guard que se asegura de que el PII no se filtra:

```python
guard = Guard(name='pii_guard').use(
    PIIDetector(
        on_fail=OnFailAction.EXCEPTION
    ),
)

try:
    guard.validate("can you tell me what orders i've placed in the last 3 months? my name is Hank Tate and my phone number is 555-123-4567")
except Exception as e:
    print(e)
```

Vemos como lo detecta

![pasted_image_20260504201204.png](/images/pasted_image_20260504201204.png)
### Ejecutando el servidor Guardrails

> Las instrucciones para instalar el servidor se pueden ver en: [Instructions to install guardrails server](/Safe%20and%20reliable%20AI%20via%20guardrails/Comandos/Comandos%20Guardrails.md#instructions-to-install-guardrails-server)

```python
guarded_client = OpenAI(base_url='http://localhost:8000/guards/pii_guard/openai/v1/')

guarded_rag_chatbot = RAGChatWidget(
    client=guarded_client,
    system_message=system_message,
    vector_db=vector_db,
)
```


```python
guarded_rag_chatbot.display()
```


```python
# Copy and paste this prompt into the chatbot above:
"""
can you tell me what orders i've placed in the last 3 months? my name is hank tate and my phone number is 555-123-4567
"""
```

![pasted_image_20260504201319.png](/images/pasted_image_20260504201319.png)

Ahroa da error a la hora de guardar el historial

```python
guarded_rag_chatbot.messages
```

![pasted_image_20260504201342.png](/images/pasted_image_20260504201342.png)

Y como vemos ahora no lo guarda

## Real Time Stream Validation

> A veces igualmente la informacion se cuela o la IA contesta con PII, para ello tenemos esta solucion

> Aqui no usamos lo anterior, lo descargamos  de un hub de guardrails que funciona parecido

Primero preparamos un nuevo guard con el guard `pii_entities`

```python
from guardrails.hub import DetectPII

guard = Guard().use(
    DetectPII(pii_entities=["PHONE_NUMBER", "EMAIL_ADDRESS"], on_fail="fix")
)
```


Ahora vamos a probarlo con un ejemplo y con el for de abajo vamos a ir viendo como valida el texto, (lo de arriba es un prompt a un modelo de ejemplo):

```python
from IPython.display import clear_output

validated_llm_req = guard(
    model="gpt-3.5-turbo",
    messages=[
        {"role": "system", "content": "You are a chatbot."},
        {
            "role": "user",
            "content": "Write a short 2-sentence paragraph about an unnamed protagonist while interspersing some made-up 10 digit phone numbers for the protagonist.",
        },
    ],
    stream=True,
)

validated_output = ""
for chunk in validated_llm_req:
    clear_output(wait=True)
    validated_output = "".join([validated_output, chunk.validated_output])
    print(validated_output)
    time.sleep(1)
```

![pasted_image_20260504201749.png](/images/pasted_image_20260504201749.png)

como vemos funciona y lo hace anonimo