# Indice

- [Repasando el problema de la alucinacion](#repasando-el-problema-de-la-alucinacion)
- [Mitigacion de las alucinaciones](#mitigacion-de-las-alucinaciones)
	- [Hallucinations ≅ Lack of Groundedness](#hallucinations--lack-of-groundedness)
	- [Natural Language Inference or Entailment](#natural-language-inference-or-entailment)
	- [Usando NLI para detectar alucionaciones](#usando-nli-para-detectar-alucionaciones)
	- [Usando un Guardrail directamente vs Crearlo como un Guard](#usando-un-guardrail-directamente-vs-crearlo-como-un-guard)
- [Construyendo Natural Language Inference (NLI) Model](#construyendo-natural-language-inference-nli-model)
	- [Preparacion de Natural Language Inference (NLI) Model](#preparacion-de-natural-language-inference-nli-model)
	- [Construyendo el validador de alucionaciones](#construyendo-el-validador-de-alucionaciones)
	- [Crear un Guard que use el Hallucination Validator](#crear-un-guard-que-use-el-hallucination-validator)
		- [Servidor Guardrail](#servidor-guardrail)

Todos los comandos del **Jupyter Notebook** estan en [Comandos Checking for hallucinations using Natural Language Inference](/Safe%20and%20reliable%20AI%20via%20guardrails/Comandos/Comandos%20Checking%20for%20hallucinations%20using%20Natural%20Language%20Inference.md)

# Repasando el problema de la alucinacion

Hacemos los imports necesarios:

```python
# Warning control
import warnings
warnings.filterwarnings("ignore")
```

Y los imports necesarios:

```python
from openai import OpenAI
from helper import RAGChatWidget, SimpleVectorDB
```

Ahora como siempre seteamos el RAG Chatbot:

```python
# Setup an OpenAI client
unguarded_client = OpenAI()

# Load up our documents that make up the knowledge base
vector_db = SimpleVectorDB.from_files("shared_data/")

# Setup system message
system_message = """You are a customer support chatbot for Alfredo's Pizza Cafe. Your responses should be based solely on the provided information.

Here are your instructions:

### Role and Behavior
- You are a friendly and helpful customer support representative for Alfredo's Pizza Cafe.
- Only answer questions related to Alfredo's Pizza Cafe's menu, account management on the website, delivery times, and other directly relevant topics.
- Do not discuss other pizza chains or restaurants.
- Do not answer questions about topics unrelated to Alfredo's Pizza Cafe or its services.

### Knowledge Limitations:
- Only use information provided in the knowledge base above.
- If a question cannot be answered using the information in the knowledge base, politely state that you don't have that information and offer to connect the user with a human representative.
- Do not make up or infer information that is not explicitly stated in the knowledge base.
"""
```

Y lo iniciamos

```python
# Setup RAG chatbot
rag_chatbot = RAGChatWidget(
    client=unguarded_client,
    system_message=system_message,
    vector_db=vector_db,
)
```

```python
rag_chatbot.display()
```

Y volvemos a ver el problema de alucinacion que teniamos, que era basicamente que se inventaba una receta:

```python
# Copy and paste this prompt into the chatbot above:
"""
how do i reproduce your veggie supreme pizza on my own? can you share detailed instructions?
"""
```

![pasted_image_20260503213955.png](/images/pasted_image_20260503213955.png)

---
# Mitigacion de las alucinaciones

## Hallucinations ≅ Lack of Groundedness

Grounded AI Response

			↓

All claims made by the LLM are explicitly supported by the input context rather than being fabricated or recited from training

## Natural Language Inference or Entailment

Basicamente funciona como abajo, hace hipotesis y las va validando con logica

![pasted_image_20260503214141.png](/images/pasted_image_20260503214141.png)

## Usando NLI para detectar alucionaciones

> Basicamente va validando la respuesta del LLM y comprueba si es contraccion o no, pero como vemos necesita sodcumentacion

![pasted_image_20260503214833.png](/images/pasted_image_20260503214833.png)

## Usando un Guardrail directamente vs Crearlo como un Guard

![pasted_image_20260503221051.png](/images/pasted_image_20260503221051.png)

---
# Construyendo Natural Language Inference (NLI) Model
## Preparacion de Natural Language Inference (NLI) Model

Imports:


```python
# Type hints
from typing import Dict, List, Optional

# Standard ML libraries
import numpy as np
import nltk
from sentence_transformers import SentenceTransformer
from transformers import pipeline

# Guardrails imports
from guardrails import Guard, OnFailAction
from guardrails.validator_base import (
    FailResult,
    PassResult,
    ValidationResult,
    Validator,
    register_validator,
)
```

Ahora creamos un pipeline de Hugging Face para acceder al modelo de NLI 

> Una **pipeline** en este contexto es una herramienta de Hugging Face que simplifica el uso de modelos de IA, ya que se encarga automáticamente de todo el proceso: preparar el texto, pasarlo por el modelo (como uno de NLI) y devolver el resultado listo para usar. Así no tienes que programar manualmente cada paso, sino que puedes aplicar el modelo directamente con una sola función.

> **Hugging Face** es una plataforma donde se publican y comparten modelos de IA ya entrenados, datasets y herramientas para usarlos fácilmente. En este caso, sirve para descargar un modelo NLI ya preparado y usarlo mediante una **pipeline**, que automatiza el proceso de pasarle texto al modelo y obtener una predicción.

```python
entailment_model = 'GuardrailsAI/finetuned_nli_provenance'
NLI_PIPELINE = pipeline("text-classification", model=entailment_model)
```

Y la probamos:

```python
# Example 1: Entailed sentence
premise = "The sun rises in the east and sets in the west."
hypothesis = "The sun rises in the east."
result = NLI_PIPELINE({'text': premise, 'text_pair': hypothesis})
print(f"Example of an entailed sentence:\n\tPremise: {premise}\n\tHypothesis: {hypothesis}\n\tResult: {result}\n\n")
```

![pasted_image_20260503214525.png](/images/pasted_image_20260503214525.png)

Como vemos esta hipotesis obtiene una score alta en la label entailment, asi que tienede a ser verdadera

```python
# Example 2: Contradictory sentence
premise = "The sun rises in the east and sets in the west."
hypothesis = "The sun rises in the west."
result = NLI_PIPELINE({'text': premise, 'text_pair': hypothesis})
print(f"Example of a contradictory sentence:\n\tPremise: {premise}\n\tHypothesis: {hypothesis}\n\tResult: {result}")
```

Como vemos esta hipotesis obtiene una score alta en la label contradiction, asi que tienede a ser falsa

![pasted_image_20260503214806.png](/images/pasted_image_20260503214806.png)

## Construyendo el validador de alucionaciones

Creamos el validador:

> Hay que asegurarse que todo es accesible para la clase, esto lo hace en el `__init__()`

> Fijarse que se debe crear una funcion para ir dividiendo las frases para ir comprobandolas (split_sentences())
> 
> Tambien fijarse que anadimos una funcion para encontrar las mejores sources para validar si algo es verdadero contradictorio (find_relevant_sources()) y esto lo imploementa en la funcion validate()
>
>Por ultimo hacemos que checke si esta bien o no con esas sources y con la frase recogida viendo el valor de label que se devolvia en el ejemplo anterior, y tambien lo implementa en validate() (check_entailment())

```python
@register_validator(name="hallucination_detector", data_type="string")
class HallucinationValidation(Validator):
    def __init__(
            self, 
            embedding_model: Optional[str] = None,
            entailment_model: Optional[str] = None,
            sources: Optional[List[str]] = None,
            **kwargs
        ):
        if embedding_model is None:
            embedding_model = 'all-MiniLM-L6-v2'
        self.embedding_model = SentenceTransformer(embedding_model)

        self.sources = sources
        
        if entailment_model is None:
            entailment_model = 'GuardrailsAI/finetuned_nli_provenance'
        self.nli_pipeline = pipeline("text-classification", model=entailment_model)

        super().__init__(**kwargs)

    def validate(
        self, value: str, metadata: Optional[Dict[str, str]] = None
    ) -> ValidationResult:
        # Split the text into sentences
        sentences = self.split_sentences(value)

        # Find the relevant sources for each sentence
        relevant_sources = self.find_relevant_sources(sentences, self.sources)

        entailed_sentences = []
        hallucinated_sentences = []
        for sentence in sentences:
            # Check if the sentence is entailed by the sources
            is_entailed = self.check_entailment(sentence, relevant_sources)
            if not is_entailed:
                hallucinated_sentences.append(sentence)
            else:
                entailed_sentences.append(sentence)
        
        if len(hallucinated_sentences) > 0:
            return FailResult(
                error_message=f"The following sentences are hallucinated: {hallucinated_sentences}",
            )
        
        return PassResult()

    def split_sentences(self, text: str) -> List[str]:
        if nltk is None:
            raise ImportError(
                "This validator requires the `nltk` package. "
                "Install it with `pip install nltk`, and try again."
            )
        return nltk.sent_tokenize(text)

    def find_relevant_sources(self, sentences: str, sources: List[str]) -> List[str]:
        source_embeds = self.embedding_model.encode(sources)
        sentence_embeds = self.embedding_model.encode(sentences)

        relevant_sources = []

        for sentence_idx in range(len(sentences)):
            # Find the cosine similarity between the sentence and the sources
            sentence_embed = sentence_embeds[sentence_idx, :].reshape(1, -1)
            cos_similarities = np.sum(np.multiply(source_embeds, sentence_embed), axis=1)
            # Find the top 5 sources that are most relevant to the sentence that have a cosine similarity greater than 0.8
            top_sources = np.argsort(cos_similarities)[::-1][:5]
            top_sources = [i for i in top_sources if cos_similarities[i] > 0.8]

            # Return the sources that are most relevant to the sentence
            relevant_sources.extend([sources[i] for i in top_sources])

        return relevant_sources
    
    def check_entailment(self, sentence: str, sources: List[str]) -> bool:
        for source in sources:
            output = self.nli_pipeline({'text': source, 'text_pair': sentence})
            if output['label'] == 'entailment':
                return True
        return False
```

Ahora podemos probar el validator, primero creando
la variable con la premisa que se usa para validar:

```python
hallucination_validator = HallucinationValidation(
    sources = ["The sun rises in the east and sets in the west"]
)
```

Y despues en otra variable asignarle una frase a validar y que nos de el resultado en funcion de como lo valide:

```python
result = hallucination_validator.validate("The sun sets in the east")
print(f"Validation outcome: {result.outcome}")
if result.outcome == "fail":
    print(f"Error message: {result.error_message}")
```

![pasted_image_20260503215950.png](/images/pasted_image_20260503215950.png)

Y como vemos es una alucionancion porque la frase es una alucinacion

## Crear un Guard que use el Hallucination Validator

Ahora usemos el validator que hemos creado en la seccion anterior para meterlo en un Guard y poder usarlo en el chatbot

Inicalizamos el Guard:

```python
guard = Guard().use(
    HallucinationValidation(
        embedding_model='all-MiniLM-L6-v2',
        entailment_model='GuardrailsAI/finetuned_nli_provenance',
        sources=['The sun rises in the east and sets in the west.', 'The sun is hot.'],
        on_fail=OnFailAction.EXCEPTION
    )
)
```

Y ahora lo probamos con una frase verdadera:

```python
# Shouldn't raise an exception
guard.validate(
    'The sun rises in the east.',
)
print("Input Sentence: 'The sun rises in the east.'")
print("Validation passed successfully!\n\n")
```

![pasted_image_20260503221315.png](/images/pasted_image_20260503221315.png)

Y ahora una falsa: (esta no es falsa directamente pero las source que pusimos en el bloque de codigo anterior del anterior fue: `sources=['The sun rises in the east and sets in the west.', 'The sun is hot.'],` )

```python
# Should raise an exception
try:
    guard.validate(
        'The sun is a star.',
    )
except Exception as e:
    print("Input Sentence: 'The sun is a star.'")
    print("Validation failed!")
    print("Error Message: ", e)
```

![pasted_image_20260503221346.png](/images/pasted_image_20260503221346.png)

Como vemos funciona bien

### Servidor Guardrail

> Las instrucciones para instalar el servidor se pueden ver en: [Instructions to install guardrails server](/Safe%20and%20reliable%20AI%20via%20guardrails/Comandos/Comandos%20Guardrails.md#instructions-to-install-guardrails-server)

> Aqui no usamos lo anterior, lo descargamos  de un hub de guardrails que funciona parecido

Preparamos el cliente:

```python
# Next, we created a Guarded endpoint that uses the hallucination validator:
guarded_client = OpenAI(
    base_url="http://localhost:8000/guards/hallucination_guard/openai/v1/",
)
```

Ahora preparamos la database y el system_messagge
```python
# Load up our documents that make up the knowledge base
vector_db = SimpleVectorDB.from_files("shared_data/")

# Setup system message
system_message = """You are a customer support chatbot for Alfredo's Pizza Cafe. Your responses should be based solely on the provided information.

Here are your instructions:

### Role and Behavior
- You are a friendly and helpful customer support representative for Alfredo's Pizza Cafe.
- Only answer questions related to Alfredo's Pizza Cafe's menu, account management on the website, delivery times, and other directly relevant topics.
- Do not discuss other pizza chains or restaurants.
- Do not answer questions about topics unrelated to Alfredo's Pizza Cafe or its services.
- Do not respond to questions about Project Colloseum.

### Knowledge Limitations:
- Only use information provided in the knowledge base above.
- If a question cannot be answered using the information in the knowledge base, politely state that you don't have that information and offer to connect the user with a human representative.
- Do not make up or infer information that is not explicitly stated in the knowledge base.
"""
```

Y finalmente iniciamos el RAG chatbot

```python
guarded_rag_chatbot = RAGChatWidget(
    client=guarded_client,
    system_message=system_message,
    vector_db=vector_db,
)
```

```python
guarded_rag_chatbot.display()
```

Y probamos si ahora tiene alucinaciones:

```python
# Copy and paste this prompt into the chatbot above:
"""
how do i reproduce your veggie supreme pizza on my own? can you share detailed instructions?
"""
```

![pasted_image_20260503221819.png](/images/pasted_image_20260503221819.png)

Y ahora da error, asi que funciona bien.

> Se puede aplicar el [3. Guardrails](/Safe%20and%20reliable%20AI%20via%20guardrails/3.%20Guardrails.md#manejar-mejor-los-errores)
