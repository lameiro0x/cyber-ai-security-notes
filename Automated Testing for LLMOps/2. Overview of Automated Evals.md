# Indice

- [Parte teorica](<#parte-teorica>)
	- [SW Tradicional vs LLM](<#sw-tradicional-vs-llm>)
	- [Model Evaluations (Evals)](<#model-evaluations-evals>)
		- [What and When](<#what-and-when>)
	- [Evals especificos del entorno](<#evals-especificos-del-entorno>)
- [Parte Practica](<#parte-practica>)
	- [Demo de la Aplicacion Quiz Generator](<#demo-de-la-aplicacion-quiz-generator>)
	- [Preparacion](<#preparacion>)
		- [Creando la aplicacion](<#creando-la-aplicacion>)
	- [Evalucaciones](<#evalucaciones>)
	- [Ejecutando evaluaciones en la CircleCI pipeline](<#ejecutando-evaluaciones-en-la-circleci-pipeline>)
		- [El archivo de configuracion](<#el-archivo-de-configuracion>)
		- [Ejecutar evals por cada commit](<#ejecutar-evals-por-cada-commit>)


Todos los comandos del **Jupyter Notebook** estan en [Comandos Overview of Automated Evals](<./Comandos/Comandos Overview of Automated Evals.md>)
# Parte teorica

## SW Tradicional vs LLM

> La principal diferencia es que no sabemos que outputs podemos recibir

![pasted_image_20260505160952.png](<../images/pasted_image_20260505160952.png>)

## Model Evaluations (Evals)

![pasted_image_20260505161236.png](<../images/pasted_image_20260505161236.png>)

### What and When

**What** should you evaluate?
- Context adherence
- Context relevance
- Correctness
- Bias and toxicity

**When** should you evaluate?
- After every change (bug fixes, feature updates, data changes)
- Pre-deployment (merges to production branch, end of sprint, prior to shipping hotfix)
- Post-deployment (on demand based on business needs)

## Evals especificos del entorno

![pasted_image_20260505163238.png](<../images/pasted_image_20260505163238.png>)

---
# Parte Practica

## Demo de la Aplicacion Quiz Generator

> Basicamente una App LLM que generame preguntas de Quiz en base a un tema pedido por el usuario

![pasted_image_20260505161546.png](<../images/pasted_image_20260505161546.png>)

## Preparacion

Primero los inputs para los warnings y la bibliotecas de testing y para montar la App y sus keys:

```python
from utils import get_circle_api_key
cci_api_key = get_circle_api_key()
```

```python
from utils import get_gh_api_key
gh_api_key = get_gh_api_key()
```

```python
from utils import get_openai_api_key
openai_api_key = get_openai_api_key()
```

Tambien preparamos un repositorio con su rama

```python
from utils import get_repo_name
course_repo = get_repo_name()
course_repo
```

```python
from utils import get_branch
course_branch = get_branch()
course_branch
```

### Creando la aplicacion

Primero vamos a crear la plantilla 

![pasted_image_20260505161902.png](<../images/pasted_image_20260505161902.png>)

Las primeras plantillas son las de las "entidades" (usuario y aplicacion):

```python
human_template  = "{question}"
```

```python
quiz_bank = """1. Subject: Leonardo DaVinci
   Categories: Art, Science
   Facts:
    - Painted the Mona Lisa
    - Studied zoology, anatomy, geology, optics
    - Designed a flying machine
  
2. Subject: Paris
   Categories: Art, Geography
   Facts:
    - Location of the Louvre, the museum where the Mona Lisa is displayed
    - Capital of France
    - Most populous city in France
    - Where Radium and Polonium were discovered by scientists Marie and Pierre Curie

3. Subject: Telescopes
   Category: Science
   Facts:
    - Device to observe different objects
    - The first refracting telescopes were invented in the Netherlands in the 17th Century
    - The James Webb space telescope is the largest telescope in space. It uses a gold-berillyum mirror

4. Subject: Starry Night
   Category: Art
   Facts:
    - Painted by Vincent van Gogh in 1889
    - Captures the east-facing view of van Gogh's room in Saint-Rémy-de-Provence

5. Subject: Physics
   Category: Science
   Facts:
    - The sun doesn't change color during sunset.
    - Water slows the speed of light
    - The Eiffel Tower in Paris is taller in the summer than the winter due to expansion of the metal."""
```

Y ahora hacemos la plantilla del prompt, donde lo combinamos con lo anterior:

```python
delimiter = "####"

prompt_template = f"""
Follow these steps to generate a customized quiz for the user.
The question will be delimited with four hashtags i.e {delimiter}

The user will provide a category that they want to create a quiz for. Any questions included in the quiz
should only refer to the category.

Step 1:{delimiter} First identify the category user is asking about from the following list:
* Geography
* Science
* Art

Step 2:{delimiter} Determine the subjects to generate questions about. The list of topics are below:

{quiz_bank}

Pick up to two subjects that fit the user's category. 

Step 3:{delimiter} Generate a quiz for the user. Based on the selected subjects generate 3 questions for the user using the facts about the subject.

Use the following format for the quiz:
Question 1:{delimiter} <question 1>

Question 2:{delimiter} <question 2>

Question 3:{delimiter} <question 3>

"""
```

Ahora vamos a uasr langchain para construir el prompt:

```python
from langchain.prompts import ChatPromptTemplate
chat_prompt = ChatPromptTemplate.from_messages([("human", prompt_template)])
```

Y lo vemos:

```python
# print to observe the content or generated object
chat_prompt
```

![pasted_image_20260505162342.png](<../images/pasted_image_20260505162342.png>)

Ahora vamos montar el LLM:

```python
from langchain.chat_models import ChatOpenAI
llm = ChatOpenAI(model="gpt-3.5-turbo", temperature=0)
llm
```

Montamos el parser para coger el output del LLM y pasarlo a String

```python
# parser
from langchain.schema.output_parser import StrOutputParser
output_parser = StrOutputParser()
output_parser
```

Y ahora conectamos todo:

```python
chain = chat_prompt | llm | output_parser
chain
```

Y ahora creamos la funcion `assistant_chain()` para hacerlo todo funcionar:

```python
# taking all components and making reusable as one piece
def assistant_chain(
    system_message,
    human_template="{question}",
    llm=ChatOpenAI(model="gpt-3.5-turbo", temperature=0),
    output_parser=StrOutputParser()):
  
  chat_prompt = ChatPromptTemplate.from_messages([
      ("system", system_message),
      ("human", human_template),
  ])
  return chat_prompt | llm | output_parser
```

## Evalucaciones

Creamos una funcion, para ver si hay una serie de palabras que esperamos que haya en la respuesta:

```python
def eval_expected_words(
    system_message,
    question,
    expected_words,
    human_template="{question}",
    llm=ChatOpenAI(model="gpt-3.5-turbo", temperature=0),
    output_parser=StrOutputParser()):
    
  assistant = assistant_chain(
      system_message,
      human_template,
      llm,
      output_parser)
    
  
  answer = assistant.invoke({"question": question})
    
  print(answer)
    
  assert any(word in answer.lower() \
             for word in expected_words), \
    f"Expected the assistant questions to include \
    '{expected_words}', but it did not"
```

Ahora lo preparamos para testear:

```python
question  = "Generate a quiz about science."
expected_words = ["davinci", "telescope", "physics", "curie"]
```

Creamos el eval:

```python
eval_expected_words(
    prompt_template,
    question,
    expected_words
)
```

![pasted_image_20260505162721.png](<../images/pasted_image_20260505162721.png>)

Vamos a crear un eval ahora para comprobar si hace bien los rechazos:

```python
def evaluate_refusal(
    system_message,
    question,
    decline_response,
    human_template="{question}", 
    llm=ChatOpenAI(model="gpt-3.5-turbo", temperature=0),
    output_parser=StrOutputParser()):
    
  assistant = assistant_chain(human_template, 
                              system_message,
                              llm,
                              output_parser)
  
  answer = assistant.invoke({"question": question})
  print(answer)
  
  assert decline_response.lower() in answer.lower(), \
    f"Expected the bot to decline with \
    '{decline_response}' got {answer}"
```

Lo preparamos para testear:

```python
question  = "Generate a quiz about Rome."
decline_response = "I'm sorry"
```

Creamos el eval:

```python
evaluate_refusal(
    prompt_template,
    question,
    decline_response
)
```

![pasted_image_20260505163020.png](<../images/pasted_image_20260505163020.png>)

![pasted_image_20260505163123.png](<../images/pasted_image_20260505163123.png>)

Da error ya que el eval espera que hay un rechazo con la frase "I'm sorry" pero no la hay ya que con eso prompt puede responder bien, entonces el eval da error

## Ejecutando evaluaciones en la CircleCI pipeline

> Esta parte no esta pensando para hacerla en Jupyter Notebook, esta pensada mas para hacerla en local

> Aqui estamos viendo basicamente lo que vimos en la seccion anterior pero adaptado a montarlo como aplicacion en archivos de python con algunos pocos cambios 

Volvemos a crear la Apliccion en un archivo `app.py`:

```python
%%writefile app.py
from langchain.prompts                import ChatPromptTemplate
from langchain.chat_models            import ChatOpenAI
from langchain.schema.output_parser   import StrOutputParser

delimiter = "####"

quiz_bank = """1. Subject: Leonardo DaVinci
   Categories: Art, Science
   Facts:
    - Painted the Mona Lisa
    - Studied zoology, anatomy, geology, optics
    - Designed a flying machine
  
2. Subject: Paris
   Categories: Art, Geography
   Facts:
    - Location of the Louvre, the museum where the Mona Lisa is displayed
    - Capital of France
    - Most populous city in France
    - Where Radium and Polonium were discovered by scientists Marie and Pierre Curie

3. Subject: Telescopes
   Category: Science
   Facts:
    - Device to observe different objects
    - The first refracting telescopes were invented in the Netherlands in the 17th Century
    - The James Webb space telescope is the largest telescope in space. It uses a gold-berillyum mirror

4. Subject: Starry Night
   Category: Art
   Facts:
    - Painted by Vincent van Gogh in 1889
    - Captures the east-facing view of van Gogh's room in Saint-Rémy-de-Provence

5. Subject: Physics
   Category: Science
   Facts:
    - The sun doesn't change color during sunset.
    - Water slows the speed of light
    - The Eiffel Tower in Paris is taller in the summer than the winter due to expansion of the metal.
"""

system_message = f"""
Follow these steps to generate a customized quiz for the user.
The question will be delimited with four hashtags i.e {delimiter}

The user will provide a category that they want to create a quiz for. Any questions included in the quiz
should only refer to the category.

Step 1:{delimiter} First identify the category user is asking about from the following list:
* Geography
* Science
* Art

Step 2:{delimiter} Determine the subjects to generate questions about. The list of topics are below:

{quiz_bank}

Pick up to two subjects that fit the user's category. 

Step 3:{delimiter} Generate a quiz for the user. Based on the selected subjects generate 3 questions for the user using the facts about the subject.

Use the following format for the quiz:
Question 1:{delimiter} <question 1>

Question 2:{delimiter} <question 2>

Question 3:{delimiter} <question 3>

Additional rules:

- Only use explicit matches for the category, if the category is not an exact match to categories in the quiz bank, answer that you do not have information.
- If the user asks a question about a subject you do not have information about in the quiz bank, answer "I'm sorry I do not have information about that".
"""

"""
  Helper functions for writing the test cases
"""

def assistant_chain(
    system_message=system_message,
    human_template="{question}",
    llm=ChatOpenAI(model="gpt-3.5-turbo", temperature=0),
    output_parser=StrOutputParser()):

  chat_prompt = ChatPromptTemplate.from_messages([
      ("system", system_message),
      ("human", human_template),
  ])
  return chat_prompt | llm | output_parser

```

Y creamos un archivo `evals.py`:

> En estos se anadieron casos de tests al final

```python
%%writefile test_assistant.py
from app import assistant_chain
from app import system_message
from langchain.prompts                import ChatPromptTemplate
from langchain.chat_models            import ChatOpenAI
from langchain.schema.output_parser   import StrOutputParser

import os

from dotenv import load_dotenv, find_dotenv
_ = load_dotenv(find_dotenv())

def eval_expected_words(
    system_message,
    question,
    expected_words,
    human_template="{question}",
    llm=ChatOpenAI(model="gpt-3.5-turbo", temperature=0),
    output_parser=StrOutputParser()):

  assistant = assistant_chain(system_message)
  answer = assistant.invoke({"question": question})
  print(answer)
    
  assert any(word in answer.lower() \
             for word in expected_words), \
    f"Expected the assistant questions to include \
    '{expected_words}', but it did not"

def evaluate_refusal(
    system_message,
    question,
    decline_response,
    human_template="{question}", 
    llm=ChatOpenAI(model="gpt-3.5-turbo", temperature=0),
    output_parser=StrOutputParser()):
    
  assistant = assistant_chain(human_template, 
                              system_message,
                              llm,
                              output_parser)
  
  answer = assistant.invoke({"question": question})
  print(answer)
  
  assert decline_response.lower() in answer.lower(), \
    f"Expected the bot to decline with \
    '{decline_response}' got {answer}"

"""
  Test cases
"""

def test_science_quiz():
  
  question  = "Generate a quiz about science."
  expected_subjects = ["davinci", "telescope", "physics", "curie"]
  eval_expected_words(
      system_message,
      question,
      expected_subjects)

def test_geography_quiz():
  question  = "Generate a quiz about geography."
  expected_subjects = ["paris", "france", "louvre"]
  eval_expected_words(
      system_message,
      question,
      expected_subjects)

def test_refusal_rome():
  question  = "Help me create a quiz about Rome"
  decline_response = "I'm sorry"
  evaluate_refusal(
      system_message,
      question,
      decline_response)
```

### El archivo de configuracion 

Ejemplo de chatgpt: (no aplicado a este ejemplo concreto)

```yml
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:21.0
    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: Dar permisos a Maven Wrapper
          command: chmod +x mvnw

      - run:
          name: Compilar proyecto
          command: ./mvnw clean compile

      - run:
          name: Ejecutar tests
          command: ./mvnw test

      - store_test_results:
          path: target/surefire-reports

      - store_artifacts:
          path: target

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
```

### Ejecutar evals por cada commit

Pusheamos los archivos al repositorio:

```python
from utils import push_files
push_files(course_repo, course_branch, ["app.py", "test_assistant.py"])
```

Y triguereaos el CircleCI pipeline;

```python
from utils import trigger_commit_evals
trigger_commit_evals(course_repo, course_branch, cci_api_key)
```

Y nos dara un link donde podemos ver donde hace los evals:

![pasted_image_20260505164246.png](<../images/pasted_image_20260505164246.png>)

![pasted_image_20260505164304.png](<../images/pasted_image_20260505164304.png>)